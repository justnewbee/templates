import styled from 'styled-components';

export const ScKeyValue = styled.div`
  margin: 0.5rem 0 1.5rem;
  font-size: 0.9em;
`;

export const ScKeyValueK = styled.span`
  display: inline-block;
  min-width: 5rem;
  opacity: 0.5;
  font-variant: all-small-caps;
`;

export const ScKeyValueV = styled.span`
  margin-left: 0.5rem;
  
  i {
    margin-right: 0.4rem;
    font-size: 1.05em;
  }
`;
