import styled from 'styled-components';

export default styled.span`
  display: inline-block;
  padding-inline: var(--ifm-code-padding-horizontal);
  border-radius: var(--ifm-code-border-radius);
  font-size: 0.85em;
  font-weight: 500;
  vertical-align: middle;
`;
